[package]
name = "archive_iso-core"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
archive_common = { path = "../common" }
jni = "0.21"
isomage = "0.1"
